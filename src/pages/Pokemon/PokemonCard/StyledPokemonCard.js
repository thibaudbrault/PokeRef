import styled from 'styled-components';
import { Subtitle, Title } from '../../../components/BaseStyles/Headings';

export const PokemonTitle = styled(Title)`
	font-size: 4rem;
	text-align: center;
`;

export const PokemonSubtitle = styled(Subtitle)`
	font-size: 1.5rem;
	text-align: center;
`;
